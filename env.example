# Authentication Configuration
# Copy this file to .env and fill in your values

# API Token (optional - will use default if not set)
EPUTS_TOKEN=your_token_here

# API Base URL (optional - will use default if not set)
API_BASE_URL=http://91.227.17.139/services/react/api

# Project settings (optional - will use defaults if not set)
PROJECT=98_spb
SERVICE=eputs

# Authentication credentials (for automatic token refresh)
USERNAME=your_username
PASSWORD=your_password
